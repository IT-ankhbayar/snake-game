import { Point } from "../types";

export const GRID_SIZE = 20;
export const CANVAS_SIZE = 400;
export const INITIAL_SNAKE: Point[] = [{ x: 10, y: 10 }];
export const INITIAL_DIRECTION: Point = { x: 0, y: 0 };
export const GAME_SPEED = 120; // 120ms